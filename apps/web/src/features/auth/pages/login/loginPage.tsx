import { LoginForm } from "./components/loginForm";

export function LoginPage() {
  return (
    <main className="bg-linear-to-tr from-background-dark to-background w-full h-dvh flex items-center justify-center">
      <LoginForm />
    </main>
  );
}
